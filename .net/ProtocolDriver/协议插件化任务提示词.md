
# ProtocolDriver 插件化框架—基于现有仓库的增强任务提示词（可直接用于 AI 执行）

> 基础路径：`.../learning-record/.net/ProtocolDriver`  
> 你已确认 Host / Host.SDK / Plugins / Test 的作用；允许在必要时新增项目。  
> 本文为严格、逐步、可执行的增强说明，AI 工具必须按顺序、按文件/位置精确修改/新增代码，不得随意假设或删除原有代码。

---

## 目标（一次说清楚）
1. 将现有插件（Plugins/*）以动态加载方式运行于 Host 内（AssemblyLoadContext 可卸载）。  
2. Host 提供 Web API 管理插件、连接与任务。  
3. 每个连接产生唯一 `connectionId`（或 requestId），上层应用使用该 ID 发起读/写。  
4. 连接成功/失败/数据返回时，Host 向上层发送通知（SignalR / Webhook / SQLite 记录）。  
5. 所有连接配置与任务、通知记录保存在本地 SQLite（或可选 DB）。  
6. 支持热更新（上传新 DLL → Host 热载并切换），并提供回滚手段。  

---

## 总体约束（必须遵守）
- **不删除或重命名原有文件**。只能新增文件或在现有文件中追加明确位置的代码。  
- 新增项目（若必要）必须以 `ProtocolDriver.*` 前缀命名并保持与现有风格一致。  
- 任何不确定点必须报告（AI 停止并输出“等待确认”），不得自作主张。  
- 所有新增 C# 文件请放在指定目录（见下文“逐步操作”）。

---

## 一次性准备（在开始编码前由 AI 工具完成）
1. 在仓库根创建目录（若不存在）：
   - `Host.SDK/`（若已有则在其基础上追加）
   - `Host/PluginRuntime/`（或在 Host 项目下新增 `PluginRuntime` 文件夹）
   - `Host/Storage/`（SQLite 访问代码）
   - `Host/WebApi/`（若 Host 已含 WebApi，则在其 Controllers 下新增）
   - `Plugins/`（已有）
2. 在 `Host` 的运行目录准备 `plugins/` 子目录（Host 启动时从此目录加载用户上传的 DLL）。

---

## 逐步操作（严格步骤，AI 工具必须按序执行并在每一步输出新增/修改文件清单与代码片段）

### 步骤 1 — 在 Host.SDK 中定义共享接口与 DTO（新增文件）
**文件**：`Host.SDK/IProtocolDriver.cs`  
**新增内容**（完整文件）：

```csharp
using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;

namespace Host.SDK
{
    // 表示插件元信息
    public record PluginMetadata(string ProtocolName, string Version);

    // 请求与响应
    public record ProtocolRequest(string Action, IDictionary<string,string> Props, byte[]? Payload = null);
    public record ProtocolResponse(bool Success, byte[]? Payload = null, IDictionary<string, object>? Parsed = null, string? Error = null);

    public enum ConnectionState { Disconnected, Connecting, Connected, Error }

    public class ConnectionStatusChangedEventArgs : EventArgs
    {
        public string ConnectionId { get; init; } = default!;
        public ConnectionState State { get; init; }
        public string? Message { get; init; }
    }

    // 连接实例接口
    public interface IProtocolConnection : IDisposable
    {
        string ConnectionId { get; }
        IDictionary<string, string> Settings { get; }
        ConnectionState State { get; }
        event EventHandler<ConnectionStatusChangedEventArgs>? ConnectionStatusChanged;

        Task OpenAsync(CancellationToken token = default);
        Task CloseAsync(CancellationToken token = default);
        Task<ProtocolResponse> ExecuteAsync(ProtocolRequest request, CancellationToken token = default);
    }

    // 插件接口（DLL 实现此接口）
    public interface IProtocolDriver
    {
        PluginMetadata Metadata { get; }
        // 创建连接实例（设置来自 Host 保存的连接配置）
        Task<IProtocolConnection> CreateConnectionAsync(IDictionary<string,string> settings, CancellationToken token = default);
    }
}
```

**注意**：
- 请把此文件加入 `Host.SDK` 项目（如果 Host.SDK 已存在，直接追加文件并编译检查）。  
- 插件（Plugins/*）需引用 `Host.SDK`。

---

### 步骤 2 — 在 Host 中新增 PluginLoader（新增文件）
**文件**：`Host/PluginRuntime/PluginLoader.cs`  
**功能**：动态加载 `plugins/*.dll`，发现实现 `Host.SDK.IProtocolDriver` 的类型并实例化；支持卸载与热载。  

**关键实现（精确代码片段）**：

```csharp
using System;
using System.Collections.Concurrent;
using System.IO;
using System.Linq;
using System.Reflection;
using System.Runtime.Loader;
using Host.SDK;

namespace Host.PluginRuntime
{
    public class PluginHandle
    {
        public string Name { get; init; } = default!;
        public string Path { get; init; } = default!;
        public AssemblyLoadContext? LoadContext { get; set; }
        public IProtocolDriver? DriverInstance { get; set; }
        public WeakReference? WeakContextRef { get; set; }
    }

    public class PluginLoader : IDisposable
    {
        private readonly string _pluginDir;
        private readonly ConcurrentDictionary<string, PluginHandle> _handles = new();

        public PluginLoader(string pluginDir)
        {
            _pluginDir = pluginDir;
            if (!Directory.Exists(_pluginDir)) Directory.CreateDirectory(_pluginDir);
        }

        public IEnumerable<PluginHandle> ListPlugins() => _handles.Values;

        public PluginHandle? LoadPlugin(string dllPath)
        {
            var full = System.IO.Path.GetFullPath(dllPath);
            var name = System.IO.Path.GetFileNameWithoutExtension(full);

            // prevent duplicate
            if (_handles.ContainsKey(name)) return _handles[name];

            var alc = new AssemblyLoadContext(Guid.NewGuid().ToString(), isCollectible: true);
            var asm = alc.LoadFromAssemblyPath(full);
            var driverType = asm.GetTypes().FirstOrDefault(t => typeof(IProtocolDriver).IsAssignableFrom(t) && !t.IsInterface && !t.IsAbstract);
            if (driverType == null)
            {
                alc.Unload();
                return null;
            }

            var inst = (IProtocolDriver)Activator.CreateInstance(driverType)!;
            var handle = new PluginHandle { Name = name, Path = full, LoadContext = alc, DriverInstance = inst, WeakContextRef = new WeakReference(alc) };
            _handles[name] = handle;
            return handle;
        }

        public void UnloadPlugin(string name)
        {
            if (!_handles.TryRemove(name, out var handle)) return;
            try
            {
                handle.DriverInstance = null;
                handle.LoadContext?.Unload();
            }
            catch { /* log */ }
        }

        public void Dispose()
        {
            foreach (var kv in _handles.Keys.ToList()) UnloadPlugin(kv);
        }
    }
}
```

**插入位置说明**：在 Host 项目中新增 `PluginRuntime` 文件夹，并放入该文件。

---

### 步骤 3 — 在 Host 中新增 ConnectionManager（新增文件）
**文件**：`Host/PluginRuntime/ConnectionManager.cs`  
**功能**：管理多个连接实例（每个连接一个 GUID），路由请求到正确的插件连接。  

**关键实现（代码片段）**：

```csharp
using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using Host.SDK;

namespace Host.PluginRuntime
{
    public class ConnectionInstance
    {
        public string ConnectionId { get; init; } = Guid.NewGuid().ToString();
        public string PluginName { get; init; } = default!;
        public IProtocolConnection? Connection { get; set; }
        public IDictionary<string,string> Settings { get; init; } = new Dictionary<string,string>();
    }

    public class ConnectionManager : IDisposable
    {
        private readonly PluginLoader _loader;
        private readonly ConcurrentDictionary<string, ConnectionInstance> _instances = new();

        public event EventHandler<ConnectionStatusChangedEventArgs>? ConnectionStatusChanged;

        public ConnectionManager(PluginLoader loader)
        {
            _loader = loader;
        }

        public async Task<ConnectionInstance> CreateConnectionAsync(string pluginName, IDictionary<string,string> settings, CancellationToken token = default)
        {
            if (!_loader.ListPlugins().Any(p => p.Name == pluginName)) throw new Exception(\"plugin not loaded\");
            var handle = _loader.ListPlugins().First(p => p.Name == pluginName);
            var conn = await handle.DriverInstance!.CreateConnectionAsync(settings, token);
            var inst = new ConnectionInstance { PluginName = pluginName, Connection = conn, Settings = new Dictionary<string,string>(settings) };
            _instances[inst.ConnectionId] = inst;

            conn.ConnectionStatusChanged += (s, e) =>
            {
                ConnectionStatusChanged?.Invoke(this, e);
            };

            return inst;
        }

        public async Task StartConnectionAsync(string connectionId, CancellationToken token = default)
        {
            if (!_instances.TryGetValue(connectionId, out var inst)) throw new Exception(\"no such connection\");
            await inst.Connection!.OpenAsync(token);
        }

        public async Task StopConnectionAsync(string connectionId, CancellationToken token = default)
        {
            if (!_instances.TryGetValue(connectionId, out var inst)) return;
            await inst.Connection?.CloseAsync(token)!;
        }

        public async Task<ProtocolResponse> ExecuteAsync(string connectionId, ProtocolRequest req, CancellationToken token = default)
        {
            if (!_instances.TryGetValue(connectionId, out var inst)) throw new Exception(\"no such connection\");
            return await inst.Connection!.ExecuteAsync(req, token);
        }

        public void Dispose()
        {
            foreach (var kv in _instances.Values)
            {
                try { kv.Connection?.Dispose(); } catch { }
            }
        }
    }
}
```

**插入位置说明**：将此文件放在 `Host/PluginRuntime/` 中。

---

### 步骤 4 — 在 Host.Storage 中新增 SQLite 存储（新增目录与文件）
**目录**：`Host/Storage/`  
**文件**：`Host/Storage/SqliteStore.cs`（使用 `System.Data.SQLite` 或 `Microsoft.Data.Sqlite`，建议后者）

**建表 SQL（执行一次）**：

```sql
CREATE TABLE IF NOT EXISTS Connections (
  Id TEXT PRIMARY KEY,
  PluginName TEXT,
  ProtocolName TEXT,
  Host TEXT,
  Port INTEGER,
  Parameters TEXT,
  CreatedTime TEXT
);

CREATE TABLE IF NOT EXISTS Requests (
  Id TEXT PRIMARY KEY,
  ConnectionId TEXT,
  Action TEXT,
  Address INTEGER,
  DataType TEXT,
  Payload TEXT,
  CreatedTime TEXT
);

CREATE TABLE IF NOT EXISTS Notifications (
  Id INTEGER PRIMARY KEY AUTOINCREMENT,
  ConnectionId TEXT,
  EventType TEXT,
  Message TEXT,
  Timestamp TEXT
);
```

**示例 C# 保存接口（片段）**：

```csharp
using Microsoft.Data.Sqlite;
using System;

namespace Host.Storage
{
    public class SqliteStore
    {
        private readonly string _dbPath;
        public SqliteStore(string dbPath) { _dbPath = dbPath; EnsureTables(); }

        private void EnsureTables()
        {
            using var conn = new SqliteConnection($"Data Source={_dbPath}");
            conn.Open();
            var cmd = conn.CreateCommand();
            cmd.CommandText = \"\"\"<PUT SQL ABOVE HERE>\"\"\";
            cmd.ExecuteNonQuery();
        }

        public void InsertNotification(string connectionId, string eventType, string message)
        {
            using var conn = new SqliteConnection($"Data Source={_dbPath}");
            conn.Open();
            var cmd = conn.CreateCommand();
            cmd.CommandText = \"INSERT INTO Notifications (ConnectionId, EventType, Message, Timestamp) VALUES ($cid,$evt,$msg,$ts)\";
            cmd.Parameters.AddWithValue(\"$cid\", connectionId);
            cmd.Parameters.AddWithValue(\"$evt\", eventType);
            cmd.Parameters.AddWithValue(\"$msg\", message);
            cmd.Parameters.AddWithValue(\"$ts\", DateTime.UtcNow.ToString(\"o\"));
            cmd.ExecuteNonQuery();
        }
    }
}
```

**插入位置说明**：新增 `Host/Storage/SqliteStore.cs`，并在 Host Program 初始化时构造 SqliteStore 的实例（路径：`plugins/protocol_config.db`）。

---

### 步骤 5 — 在 Host.Controllers 中新增 Web API 控制器（新增或修改文件）
**文件**：`Host/Controllers/PluginController.cs`（新增）

```csharp
using Microsoft.AspNetCore.Mvc;
using Host.PluginRuntime;
using Host.Storage;

[ApiController]
[Route("api/plugins")]
public class PluginController : ControllerBase
{
    private readonly PluginLoader _loader;
    public PluginController(PluginLoader loader) { _loader = loader; }

    [HttpGet]
    public IActionResult List() => Ok(_loader.ListPlugins().Select(p => new { p.Name, p.Path }));

    [HttpPost("upload")]
    public IActionResult Upload(IFormFile file)
    {
        var target = Path.Combine("plugins", file.FileName);
        using var stream = System.IO.File.Create(target);
        file.CopyTo(stream);
        _loader.LoadPlugin(target);
        return Ok();
    }

    [HttpPost("{name}/unload")]
    public IActionResult Unload(string name) { _loader.UnloadPlugin(name); return Ok(); }
}
```

**文件**：`Host/Controllers/ConnectionController.cs`（新增）

```csharp
using Microsoft.AspNetCore.Mvc;
using Host.PluginRuntime;
using Host.Storage;

[ApiController]
[Route("api/connections")]
public class ConnectionController : ControllerBase
{
    private readonly ConnectionManager _mgr;
    private readonly SqliteStore _store;
    public ConnectionController(ConnectionManager mgr, SqliteStore store) { _mgr = mgr; _store = store; }

    [HttpPost]
    public IActionResult Create([FromBody] CreateConnectionDto dto)
    {
        var id = Guid.NewGuid().ToString();
        _store.InsertConnection(id, dto.PluginName, dto.ProtocolName, dto.Host, dto.Port, JsonSerializer.Serialize(dto.Parameters));
        return Ok(new { ConnectionId = id });
    }

    [HttpPost("{id}/start")]
    public async Task<IActionResult> Start(string id)
    {
        var cfg = _store.GetConnection(id);
        var pluginName = cfg.PluginName;
        var settings = JsonSerializer.Deserialize<Dictionary<string,string>>(cfg.Parameters);
        var inst = await _mgr.CreateConnectionAsync(pluginName, settings);
        await _mgr.StartConnectionAsync(inst.ConnectionId);
        return Ok(new { ConnectionId = inst.ConnectionId });
    }
}
```

**说明**：上述为示例，AI 必须生成 DTO (`CreateConnectionDto`) 与 Store 的 `InsertConnection/GetConnection` 方法。把控制器文件放在 `Host/Controllers/` 下。

---

### 步骤 6 — 在 Plugins/Modbus 添加 Modbus 插件实现（新增文件）
**文件**：`Plugins/Modbus/ModbusPlugin.cs`（新增）

**主要职责**：
- 引用 `Host.SDK`
- 实现 `IProtocolDriver`
- 在 `CreateConnectionAsync` 中返回实现 `IProtocolConnection`（例如 `ModbusConnection`），该对象负责：OpenAsync 建立 TCP，ExecuteAsync 根据 ProtocolRequest 组包发送并返回结果；触发 ConnectionStatusChanged 事件；Dispose 关闭 socket。

**注意**：请在插件内复用原有 `Plugins/Modbus` 项目中的实现细节或驱动代码，不要删除原实现。

---

### 步骤 7 — 通知上层（SignalR / WebSocket / Webhook）
**建议实现**：
- 在 Host 启动 SignalR Hub：`/ws/notifications`（新增类 `Host/Hubs/NotificationHub.cs`）
- 在 ConnectionManager 订阅连接状态事件并在事件到达时：
  - 写入 SQLite Notifications 表
  - 通过 `IHubContext<NotificationHub>` 推送到连接的客户端（JSON）

**示例推送格式**：

```json
{
  "event": "ConnectionSuccess",
  "connectionId": "GUID",
  "protocol": "Modbus",
  "timestamp": "2025-12-05T12:34:56Z",
  "message": "Connected to 192.168.1.11:502"
}
```

---

### 步骤 8 — 热更新流程（说明与 API）
1. 管理端上传新 DLL（POST `/api/plugins/upload`）  
2. Host 保存到 `plugins/<name>.dll`，调用 `_loader.LoadPlugin(path)`（若已加载则先 Unload 再 Load）  
3. 如果存在活动连接实例并希望切换到新插件：
   - 可先停止 connection（或进入 draining 状态）；或
   - 让新 plugin 提供向后兼容接口（推荐先停止->卸载->加载->重连）  
4. 支持回滚：Host 保留旧 DLL 备份路径，若新插件报错则 Load 回旧版本

---

### 步骤 9 — 测试计划（具体操作）
- **测试 A：插件加载**  
  1. Stop Host（若在调试）  
  2. 放置 `Plugins/Modbus.dll` 到 `plugins/` 目录  
  3. 启动 Host，访问 `GET /api/plugins`，确认列表包含 Modbus  
- **测试 B：创建连接并启动**  
  1. POST `/api/connections` 创建连接条目（Host=192.168.1.11, Port=502, PluginName=Modbus）  
  2. POST `/api/connections/{id}/start`  
  3. 监听 WebSocket `/ws/notifications`，确认收到 ConnectionSuccess  
- **测试 C：下发读取请求**  
  1. POST `/api/requests`（Body 包含 connectionId, action=read, address=100, count=1）  
  2. 等待请求完成，确认响应包含 requestId, payload, parsed 字段  
- **测试 D：热更新插件**  
  1. 上传新插件 DLL  
  2. Host 热载并重连实例，确认请求仍能正常工作或可回滚

---

### 步骤 10 — 输出与交付项（AI 在完成后必须提供）
1. 列表：新增文件名 / 修改文件名（路径）  
2. 每个新增文件的完整源码（可直接复制粘贴）  
3. 在已修改文件中每处修改的具体代码片段与插入位置（上下文）  
4. 运行脚本或命令（如何启动 Host、如何上传插件、如何执行 API 测试）  
5. 如果使用第三方 NuGet，请列出包名与版本（如 `Microsoft.Data.Sqlite`, `Microsoft.AspNetCore.SignalR`）

---

## 结束语（务必阅读）
- 请 AI 工具严格按此 .md 步骤执行；遇到任何需要人工确认的决策点（如插件内部 API 设计差异、是否强制停止连接以热更）请停止并输出“等待用户确认”。  
- 任何新增文件请放在指定目录，且必须在回复中标注路径与完整内容。

